// Pure state + geometry, with no React and no CSS. Import this when you want
// the window-manager model without the UI — server rendering, tests, or a
// completely custom shell.
export * from '../core';
